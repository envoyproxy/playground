
Provides an example of a minimal asynchronous web server.

The service is configurable by editing the python `run.py`.

With the default configuration will listen on port `8000`.

This image does not have any significant environment variables, although they
can be used with custom configuration.

Container has some other [aio-libs](https://github.com/aio-libs) for example for connecting
to `mariadb` or `postgres` servers.
