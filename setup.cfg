[tool:pytest]
python_files=tests/**/*.py
addopts=--tb=short --strict --cov=tests/py --cov=playground.control --cov-report=term --cov-report=xml:coverage/coverage.xml
testpaths=tests playground/control
norecursedirs=.git tmp*

[flake8]
# max-line-length = 20
